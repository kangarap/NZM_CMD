[package]
name = "tool"
version = "0.1.0"
edition = "2024"

[dependencies]
eframe = "0.24"
screenshots = "0.8"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
image = "0.24"
# 如果要接入真实 OCR，请添加：
# ort = "1.15"
